import { Auth } from "../components/Auth";

const Signin = () => {
  
  return (
    <div>
      <title>Login - Hotel Booking System</title>
      <Auth type="signin" />
    </div>
  );
};

export default Signin;